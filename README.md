# knit
"package" manager for Knittight [github] repositories

# How To Install

Installing knit is quite easy.
1. Download `install.sh`
2. Run chmod +x install.sh
3. Run ./install.sh
4. Run `sudo knit init` and then youre all set!
